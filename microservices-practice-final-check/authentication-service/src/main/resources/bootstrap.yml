spring:
    application:
        name: auth-serv
    cloud:
        config:
            fail-fast: true
            retry:
                maxAttempts: 20
                maxInterval: 10000
            uri: http://spring-cloud-config-server:8888
            
    
            